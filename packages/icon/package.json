{
  "name": "@tiki/icon",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup icons --format cjs,esm --dts",
    "generate": "node generateIcon.js",
    "svgr": "rm -rf src/* && npx @svgr/cli --typescript --out-dir src --ignore-existing -- assets"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tiki/tsconfig": "workspace:^"
  },
  "dependencies": {
    "@svgr/cli": "^8.1.0",
    "tsup": "^8.3.0"
  }
}